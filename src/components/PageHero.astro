---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<section class="page-hero container">
  <h1>{title}</h1>
  <p>{description}</p>
</section>
