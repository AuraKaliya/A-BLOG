{
  "name": "a-blog",
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "backend:dev": "python backend/manage.py runserver 8000",
    "backend:migrate": "python backend/manage.py migrate",
    "backend:seed-pages": "python backend/manage.py seed_pages",
    "backend:sync": "python backend/manage.py sync_articles",
    "backend:test": "python backend/manage.py test articles",
    "build": "astro check && astro build",
    "test": "playwright test --reporter=line --workers=1",
    "test:all": "npm run build && npm test && npm run backend:test",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.16.3",
    "parse5": "^7.3.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
